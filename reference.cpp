// There will be more added to this file in the future
#include <iostream>
#include <string>
#include <cmath>
#include <vector>

using namespace std;
// to take input from a vector
void input(vector<int> &v, int n)
{
	for (int i = 0; i < n; ++i)
	{
		int ele;
		cin >> ele;
		v.push_back(ele);
	}
}

int main()
{
	int T;
	cin >> T;
	while (T--)
	{
		// solution goes here
	}
	return 0;
}